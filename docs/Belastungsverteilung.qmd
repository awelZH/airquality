---
title: "Belastungsverteilung"
lightbox: true
---


Hier werden Auswertungen von jahresbezogenen, flächendeckenden Rasterdaten zur Luftbelastung und Verteilung der Wohnbevölkerung im Kanton Zürich sowie zu atmosphärischen Stickstoffenträgen in empfindliche Ökosysteme dargestellt. Die Grundlagendaten stammen aus Erhebungen ([Bundesamt für Statistik BFS](https://www.bfs.admin.ch/bfs/de/home.html)) und Modellrechnungen ([Bundesamt für Umwelt BAFU](https://www.bafu.admin.ch/bafu/de/home.html)). Die Modellergebnisse zur Luftschadstoffbelastung und den atmosphärischen Stickstoffeinträgen sind an Messwerten "kalibriert", um eine ausreichend gute Genauigkeit zu erreichen. Die Expositionsverteilung der Bevölkerung wird in einigen Darstellungen immer mit den aktuellen Grenzwerten der [Luftreinhalteverordnung des Bundes](https://www.fedlex.admin.ch/eli/cc/1986/208_208_208/de) verglichen, auch wenn diese erst ab einem bestimmten Zeitpunkt eingeführt oder angepasst wurden. Das Gleiche gilt für die [Richtwerte der Weltgesundheitsorganisation WHO](https://www.who.int/publications/i/item/9789240034228).


```{r}
# plots are built from the output data by the scripts in ../scripts (usable in the console as well)
withr::with_dir("..", {
  source("scripts/_plot_setup.R", encoding = "UTF-8")
  source("scripts/_plot_exposition.R", encoding = "UTF-8")
})
plots <- 
  plots_exposition |> 
  dplyr::arrange(parameter, plot, dplyr::desc(year)) |>
  dplyr::mutate(
    id = 1:dplyr::n(),
    markdown = dplyr::case_when(
      !is.na(year) ~ build_panel(id, year, parameter, plot, "exposition"),
      TRUE ~ NA
    )
  )
```


### Einwohner/innen mit Belastung über Grenz-/Richtwert

<!-- ```{r} -->
<!-- #| label: fig-population-over-thresh-rel -->
<!-- get_plot(plots, "population_over_thresh_share") -->
<!-- ``` -->

```{r}
#| label: fig-population-over-thresh-ts
#| fig.asp: 0.6
get_plot(plots, "population_over_thresh")
```


```{r}
p <- get_plot(plots, "population_over_thresh")
p$data |> 
  dplyr::select(year, pollutant, reference, population) |> 
  tidyr::spread(reference, population) |> 
  dplyr::arrange(pollutant, dplyr::desc(year)) |> 
  dplyr::mutate(
    `über WHO-Richtwert` = format(`über LRV-Grenzwert` + `über WHO-Richtwert`, scientific = FALSE, big.mark = "'"),
    `über LRV-Grenzwert` = format(`über LRV-Grenzwert`, scientific = FALSE, big.mark = "'"),
    `unter Grenz-/Richtwert` = format(`unter Grenz-/Richtwert`, scientific = FALSE, big.mark = "'")
  ) |> 
  dplyr::rename(
    Jahr = year,
    Schadstoff = pollutant,
    "< Grenz-/Richtwert" = `unter Grenz-/Richtwert`,
    "> LRV-Grenzwert" = `über LRV-Grenzwert`,
    "> WHO-Richtwert" = `über WHO-Richtwert`
  ) |> 
  kableExtra::kbl() |> 
  kableExtra::kable_paper(full_width = FALSE) |> 
  kableExtra::scroll_box(height = "200px")
```


### Stickstoffdioxid

::: {.panel-tabset}

`r knitr::knit(text = as.character(dplyr::filter(plots, parameter == "NO2" & plot == "distribution_histogram")$markdown))`

:::

::: {.panel-tabset}

`r knitr::knit(text = as.character(dplyr::filter(plots, parameter == "NO2" & plot == "distribution_cumulative")$markdown))`

:::

```{r}
#| label: fig-pwmean-no2
get_plot(plots, "population_weighted_mean", "NO2")
```

::: {.panel-tabset}

`r knitr::knit(text = as.character(dplyr::filter(plots, parameter == "NO2" & plot == "population_weighted_mean_map")$markdown))`

:::

### Feinstaub PM2.5

::: {.panel-tabset}

`r knitr::knit(text = as.character(dplyr::filter(plots, parameter == "PM2.5" & plot == "distribution_histogram")$markdown))`

:::

::: {.panel-tabset}

`r knitr::knit(text = as.character(dplyr::filter(plots, parameter == "PM2.5" & plot == "distribution_cumulative")$markdown))`

:::

```{r}
#| label: fig-pwmean-pm25
get_plot(plots, "population_weighted_mean", "PM2.5")
```

::: {.panel-tabset}

`r knitr::knit(text = as.character(dplyr::filter(plots, parameter == "PM2.5" & plot == "population_weighted_mean_map")$markdown))`

:::

### Feinstaub PM10

::: {.panel-tabset}

`r knitr::knit(text = as.character(dplyr::filter(plots, parameter == "PM10" & plot == "distribution_histogram")$markdown))`

:::

::: {.panel-tabset}

`r knitr::knit(text = as.character(dplyr::filter(plots, parameter == "PM10" & plot == "distribution_cumulative")$markdown))`

:::

```{r}
#| label: fig-pwmean-pm10
get_plot(plots, "population_weighted_mean", "PM10")
```

::: {.panel-tabset}

`r knitr::knit(text = as.character(dplyr::filter(plots, parameter == "PM10" & plot == "population_weighted_mean_map")$markdown))`

:::

<!-- ### Russ im Feinstaub -->

<!-- ::: {.panel-tabset} -->

<!-- `r knitr::knit(text = as.character(dplyr::filter(plots, parameter == "NO2" & plot == "distribution_histogram")$markdown))` -->

<!-- ::: -->

### Ozon

#### Typische Spitzenbelastung

Die "typische Spitzenbelastung" entspricht dem höchsten monatlichen 98%-Perzentil der Halbstundenmittelwerte.


::: {.panel-tabset}

`r knitr::knit(text = as.character(dplyr::filter(plots, parameter == "O3_max_98p_m1" & plot == "distribution_histogram")$markdown))`

:::

::: {.panel-tabset}

`r knitr::knit(text = as.character(dplyr::filter(plots, parameter == "O3_max_98p_m1" & plot == "distribution_cumulative")$markdown))`

:::

```{r}
#| label: fig-pwmean-o3p98
get_plot(plots, "population_weighted_mean", "O3_max_98p_m1")
```

::: {.panel-tabset}

`r knitr::knit(text = as.character(dplyr::filter(plots, parameter == "O3_max_98p_m1" & plot == "population_weighted_mean_map")$markdown))`

:::

#### mittlere Sommertagbelastung

Die "mittlere Sommertagbelastung" entspricht dem Durchschnitt des maximalen 8-Stunden-Mittelwerts der O3-Konzentration in den sechs aufeinanderfolgenden Monaten mit der höchsten O3-Konzentration im Sechsmonatsdurchschnitt.


::: {.panel-tabset}

`r knitr::knit(text = as.character(dplyr::filter(plots, parameter == "O3_peakseason_mean_d1_max_mean_h8gl" & plot == "distribution_histogram")$markdown))`

:::

::: {.panel-tabset}

`r knitr::knit(text = as.character(dplyr::filter(plots, parameter == "O3_peakseason_mean_d1_max_mean_h8gl" & plot == "distribution_cumulative")$markdown))`

:::

```{r}
#| label: fig-pwmean-o3peak
get_plot(plots, "population_weighted_mean", "O3_peakseason_mean_d1_max_mean_h8gl")
```

::: {.panel-tabset}

`r knitr::knit(text = as.character(dplyr::filter(plots, parameter == "O3_peakseason_mean_d1_max_mean_h8gl" & plot == "population_weighted_mean_map")$markdown))`

:::

### Stickstoffeintrag in empfindliche Ökosysteme

::: {.panel-tabset}

`r knitr::knit(text = as.character(dplyr::filter(plots, parameter == "Ndep" & plot == "distribution_histogram")$markdown))`

:::

::: {.panel-tabset}

`r knitr::knit(text = as.character(dplyr::filter(plots, parameter == "Ndep" & plot == "distribution_cumulative")$markdown))`

:::

::: {.panel-tabset}

`r knitr::knit(text = as.character(dplyr::filter(plots, parameter == "Ndep" & plot == "population_weighted_mean_map")$markdown))`

:::
