% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/emissions.R
\name{add_emission_colours}
\alias{add_emission_colours}
\title{Add plot order and colours per subsector}
\usage{
add_emission_colours(
  data,
  sector_colours = c(Dienstleistungen = "Gold", Haushalte = "Green", Industrie = "Blue",
    `Land- und Forstw.` = "Purple", Verkehr = "Gray", `natürl. Emissionen` = "natural")
)
}
\arguments{
\item{data}{Output of \code{\link[=aggregate_emissions]{aggregate_emissions()}}.}

\item{sector_colours}{Named character vector: one \code{\link[airquality.methods:pal_emissions]{airquality.methods::pal_emissions()}}
palette name per sector.}
}
\value{
\code{data} with the columns \code{sector}, \code{subsector_new}, \code{rootcol} (palette name), \code{order}
(plot order over all sectors), \code{col} (colour) in front, sorted by \code{year}, \code{pollutant},
\code{sector} and decreasing \code{emission}.
}
\description{
Within each sector, subsectors are ordered by their total emission over all years and
pollutants in \code{data} (largest first), and get a shade of the sector's colour ramp
(\code{\link[airquality.methods:pal_emissions]{airquality.methods::pal_emissions()}}). The columns are part of \code{data_emissions.csv}.
}
\keyword{internal}
