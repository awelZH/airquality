% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/emissions.R
\name{aggregate_emissions}
\alias{aggregate_emissions}
\title{Sum up emissions per year, pollutant, sector and (regrouped) subsector}
\usage{
aggregate_emissions(data, subsector_new)
}
\arguments{
\item{data}{Output of \code{\link[=prepare_emissions]{prepare_emissions()}}.}

\item{subsector_new}{Lookup table with columns \code{subsector} and \code{subsector_new}
(\code{data/meta/emikat_subsector_new.csv}).}
}
\value{
One row per \code{year}, \code{pollutant}, \code{unit}, \code{sector}, \code{subsector_new} and \code{source}, with
columns \code{year}, \code{pollutant}, \code{metric} ("Jahresmenge"), \code{unit}, \code{sector}, \code{subsector_new},
\code{emission}, \code{source}.
}
\description{
Subsectors are renamed or merged thematically with the lookup table \code{subsector_new}, which
should list every subsector; missing ones keep their name, with a warning. Small subsectors are
merged afterwards per pollutant, see \code{\link[=group_minor_subsectors]{group_minor_subsectors()}}. Groups without emission are
dropped.
}
\keyword{internal}
