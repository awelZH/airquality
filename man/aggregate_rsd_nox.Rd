% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/emissions.R
\name{aggregate_rsd_nox}
\alias{aggregate_rsd_nox}
\title{Mean RSD NOx emission per vehicle group}
\usage{
aggregate_rsd_nox(data, meta, filters, groups)
}
\arguments{
\item{data}{Output of \code{\link[=prepare_rsd]{prepare_rsd()}}.}

\item{meta}{Vehicle metadata in long format (see \code{\link[=prepare_rsd]{prepare_rsd()}}); \code{vehicle_euronorm} holds
either a Euronorm or a vehicle model year.}

\item{filters}{Filter criteria with columns \code{parameter}, \code{min}, \code{max} (uses \code{nmin}).}

\item{groups}{Grouping columns: \code{vehicle_type}, \code{vehicle_fuel_type}, \code{vehicle_euronorm};
\code{vehicle_model_year}, \code{vehicle_type}, \code{vehicle_fuel_type}; or \code{year}, \code{vehicle_fuel_type}.}
}
\value{
One row per group with \code{pollutant}, \code{metric}, the \code{groups}, \code{emission} (mean),
\code{unit}, \code{n}, \code{standarderror}, \code{nox_emission_threshold_g_per_kg_fuel} (not per year) and \code{source}.
}
\description{
Groups with fewer than \code{nmin} vehicles (from \code{filters}) get \code{NA} statistics; group
combinations that do not occur are added with \code{n = 0} (\code{\link[airquality.methods:aggregate_groups]{airquality.methods::aggregate_groups()}}).
Per Euronorm or per vehicle model year, the NOx threshold of the group is added. Per year of
measurement, all fuel types together are added as \code{vehicle_fuel_type = "all"}.
}
\keyword{internal}
