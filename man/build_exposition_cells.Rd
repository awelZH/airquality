% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/exposition.R
\name{build_exposition_cells}
\alias{build_exposition_cells}
\title{Build the cell table of the exposition from the rasters}
\usage{
build_exposition_cells(years, map_municipalities, correct_noloc = TRUE)
}
\arguments{
\item{years}{Years to read.}

\item{map_municipalities}{Municipality polygons (without foreign enclaves).}

\item{correct_noloc}{Subtract the STATPOP collector pixels (inhabitants that
cannot be located), see \code{\link[airquality.methods:read_statpop_ha]{airquality.methods::read_statpop_ha()}}.}
}
\value{
The cell table: \code{x}, \code{y}, \code{year}, \code{population}, \code{bfsnr}, \code{gemeindename} and one column per
pollutant.
}
\description{
Reads the rasters (\code{\link[=read_exposition_rasters]{read_exposition_rasters()}}), turns them into one row per inhabited cell and year,
assigns each cell its municipality and gives the STATPOP collector pixel inhabitants back to their
municipality (decisions 3–5). One step, because the aligned rasters are partly streamed from the web
(GDAL \verb{/vsicurl/}) and cannot be stored in between.
}
\keyword{internal}
