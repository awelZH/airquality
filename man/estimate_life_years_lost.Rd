% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/outcomes.R
\name{estimate_life_years_lost}
\alias{estimate_life_years_lost}
\title{Estimate the years of life lost per year and parameter, for the actual exposure and the base year}
\usage{
estimate_life_years_lost(
  expo,
  lifetable,
  meta,
  min_age,
  max_age,
  erf_shape = "log_linear"
)
}
\arguments{
\item{expo}{Population-weighted means of the canton (\code{data_exposition_weighted_means_canton.csv}),
one row per year and parameter; a missing base-year mean counts as the actual one.}

\item{lifetable}{Life tables as returned by \code{\link[=lifetable_data]{lifetable_data()}}.}

\item{meta}{Outcome metadata, one row per parameter (\code{crf}, \code{crf_lower}, \code{crf_upper},
\code{crf_conc_increment}, \code{lower_conc_threshold}).}

\item{min_age}{Youngest age affected by the exposure.}

\item{max_age}{Last age group of the life tables.}

\item{erf_shape}{Shape of the exposure-response function.}
}
\value{
Tibble with \code{year}, \code{parameter}, \code{scenario} ("actual", "base"), \code{outcome}, \code{outcome_lower} and
\code{outcome_upper}, for the years and parameters in all three inputs.
}
\description{
With the life table of \code{\link[healthiar:attribute_lifetable]{healthiar::attribute_lifetable()}}: the years of life lost by the exposure of
one year (\code{approach_exposure = "single_year"}, without newborns) from \code{min_age}, summed over the
sexes. Deterministic like \code{\link[=estimate_premature_deaths]{estimate_premature_deaths()}}: the range from the 95 \% bounds of the
relative risk.
}
\keyword{internal}
