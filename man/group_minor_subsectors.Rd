% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/emissions.R
\name{group_minor_subsectors}
\alias{group_minor_subsectors}
\title{Merge small subsectors into "verschiedene", per pollutant and sector}
\usage{
group_minor_subsectors(
  data,
  min_share = 0.05,
  max_per_sector = 4,
  other = "verschiedene"
)
}
\arguments{
\item{data}{Output of \code{\link[=aggregate_emissions]{aggregate_emissions()}}.}

\item{min_share}{Smallest mean yearly share (0–1) of a subsector with its own name.}

\item{max_per_sector}{Largest number of groups per pollutant and sector, including \code{other}.}

\item{other}{Name of the collective group.}
}
\value{
\code{data} with the same columns, summed per new \code{subsector_new}.
}
\description{
Keeps the emission plots of a pollutant readable: per pollutant and sector,
\itemize{
\item subsectors whose mean yearly share of the pollutant's total emission is below \code{min_share} go
into \code{other} (years without emission count as share 0), and
\item at most \code{max_per_sector} groups remain, including \code{other}: if there is an \code{other} group (or
more than \code{max_per_sector} subsectors), only the \code{max_per_sector - 1} largest keep their name.
}
}
\details{
The grouping is decided once per pollutant from the mean over all years in \code{data}, so the whole
time series of a pollutant shows the same groups. A subsector already called \code{other} (e.g. from
the lookup table) is part of the collective group.
}
\keyword{internal}
