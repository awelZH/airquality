% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/plot.R
\name{plot_catalog}
\alias{plot_catalog}
\title{Collect plots in a catalog for the Quarto pages}
\usage{
plot_catalog(figures, plot, names_to = character())
}
\arguments{
\item{figures}{A ggplot, a list of ggplots named by parameter or year, or a list of such lists
(names of the outer list = parameter, of the inner lists = year).}

\item{plot}{Name of the plot, e.g. "distribution_histogram".}

\item{names_to}{What the names of the list levels are: \code{character()} for a single plot, \code{"parameter"},
\code{"year"} or \code{c("parameter", "year")}.}
}
\value{
Tibble with \code{plot}, \code{parameter} and \code{year} (character, \code{NA} if not applicable) and the list
column \code{figure}, one row per plot.
}
\description{
The caller says what the names of the list mean, so the catalog does not guess its structure.
}
\keyword{internal}
