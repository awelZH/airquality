% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/emissions.R
\name{prepare_rsd}
\alias{prepare_rsd}
\title{Prepare RSD measurements: one row per vehicle with its NOx emission}
\usage{
prepare_rsd(data, meta, filters, model_year_max)
}
\arguments{
\item{data}{Raw RSD data from opendata.swiss in long format: one row per vehicle measurement
(\code{id}) and \code{parameter} (\code{velocity}, \code{acceleration}, \code{NO}, \code{CO2}, \code{CO}, \code{HC}, ...).}

\item{meta}{Vehicle metadata (\code{inst/extdata/meta/rsd_auxiliary.csv}): \code{vehicle_type},
\code{vehicle_fuel_type}, \code{vehicle_euronorm}, \code{parameter}, \code{value}, \code{source}, \code{remark}.}

\item{filters}{Filter criteria (\code{inst/extdata/meta/rsd_filters.csv}): \code{parameter}, \code{min}, \code{max}.}

\item{model_year_max}{Newest vehicle model year kept (upper bound of \code{vehicleyears}).}
}
\value{
One row per vehicle measurement with the measured concentrations as columns,
\code{vehicle_specific_power} (kW/t), the metadata columns and \code{nox_emission} (g/kg fuel);
\code{vehicle_type} and \code{vehicle_fuel_type} as factors.
}
\description{
Computes the vehicle specific power, applies the filter criteria, adds the vehicle metadata
(HBEFA NO2 fraction, NOx thresholds) and calculates the NOx emission.
}
\keyword{internal}
